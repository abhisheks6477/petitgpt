# Public runtime support V2

This is implementation support, not a trainer, model export, historical replay or new evaluation. All original files and frozen manifests remain unchanged. It supplements the accepted research-v1 source handoff and the verified P2 transport for laptop commit 504449730852fc04c27068bdfdc90f53718ad6b4. The resolved laptop baseline comparison is not reopened.

| Entry point | Implementable now | Private input needed | Genuinely missing implementation |
|---|---|---|---|
| P2 | Existing unchanged p2.py passes real input path validation | Frozen train/validation JSONL, original consumption plan, canonical Base | No adapter fix identified; execution and model equivalence untested |
| P3 supplied-frozen-input training | Training helper subset, complete ordered plan schema and exact runtime contract recovered | train/replay JSONL, ordered plan, original freeze/config, P2 step750, tokenizer | Public CLI/path adapter and explicit mode selection remain to be implemented on laptop |
| P3 full historical schedule | Source/function/callback contracts recovered | Development data, train128, original full freeze dependencies; final and later diagnostic inputs for full pipeline | Private callback integration; do not bypass historical baseline or evaluation gates |
| Fixed interpolation | Existing blend source plus preparation and relocation contract recovered | P2 step750 and P3 step320 parents | Public path/output adapter; no missing blend algorithm |
| Native export | Exact frozen inference bytes and real bundle/weight conversion operations recovered | Bound alpha075 checkpoint; optional private evidence for historical parity/review replication | Complete public builder joining operations; the old excerpt is not runnable alone |

## P2 validation

P2_REAL_DATA_PATH_VALIDATION=PASS. Return code 0. The unmodified p2.py --validate-only branch checked the full opaque Base hash, tokenizer identity, 12,000 training rows and 500 validation rows, unique/disjoint audit IDs, each row's shifted supervised targets, and the complete deterministic two-pass plan. It never created the fresh requested output directory. Every loaded src/sft module originated in the transport source closure. Python 3.10.12, torch 2.11.0+cu126, numpy 2.2.6, tokenizers 0.22.2, safetensors 0.8.0; no dependency installation.

The existing train location equals the historical train_path, so the relocated plan hash remains 20ee8fb0151ae761a9dbe2529663ff24a0295edf033be3baaa69bf8db68e3798. A future relocation may produce a different file hash while preserving every non-location field. Record both identities. Observed one-pass supervised counts: train 997,427 and validation 43,573; the 750-update plan consumes 24,000 rows and 1,994,854 targets. This establishes input/encoding/plan compatibility, not model or training reproduction.

Portable command shape (tokens stand for local bindings; the exact executed argv, environment and safety observer are private):

```text
${VENV_PYTHON} -B ${TRANSPORT}/recipes/research-v1/p2.py \
  --train-jsonl ${P2_TRAIN} --validation-jsonl ${P2_VALIDATION} \
  --base-checkpoint ${BASE_049590} --original-consumption-plan ${ORIGINAL_PLAN} \
  --out-dir ${FRESH_NONEXISTENT_MAINTENANCE_OUTPUT} --validate-only
```

Observed execution used runpy on that unchanged script inside an external guard, with cwd outside both checkout and source root, empty PYTHONPATH, disabled bytecode, empty CUDA_VISIBLE_DEVICES, and TMPDIR inside the new maintenance directory. Model/optimizer constructors, checkpoint deserialization, CUDA entry points, subprocess/network use and out-of-maintenance writes were guarded. The observer initially rejected a raw comparison of projected versus absolute launch paths before invoking p2.py; only that new observer comparison was corrected. The adapter itself did not fail or require a patch.

## P3 supplied input and import contract

Use the supplied frozen data rather than importing prepare.py or curriculum.py wholesale. Original execute.py imports checkers.check and prepare.{RUN,OUT,ROOT,P2,H,TOK,sha,rows,write,canonical}; prepare.py imports curriculum.{SEED,WORDS,build_procedural,build_plan,canonical,digest,inventory,training_values}. Importing prepare unnecessarily couples supplied-input training to mixed train/development/final procedural generation. Replace this wiring in a separately hashed adapter.

helpers/p3_plan.py contains exact SEED/FAMILIES/canonical/digest/build_plan segments with minimal standard-library imports. It needs only ordered training records' audit_id/family and replay records' audit_id. No vocabulary or template arrays are included. helpers/p3_io.py contains exact sha/rows/write/annotate functions; annotate mutates only the in-memory record passed by the caller, so pass a copy during validation and never rewrite frozen files. It imports src.chat_template.encode_chat from the pinned existing closure. helpers/p3_checkers.py contains the pure check(row,text) function and standard-library imports. It is an evaluation helper, not a training loss or procedural generator; it contains no frozen prompts, references, labels or tests.

Keep src/sft imports pinned to the accepted source closure. Exact imports, function signatures, line boundaries and direct calls appear in contracts/SYMBOL_DEPENDENCIES.json. Runtime source hashes and prior projected source identities appear in SOURCE_HASH_MAP.json; these are separate from the original FROZEN.json checks.

Excluded curriculum functions: inventory, training_values, create_payloads, task_payload, reference, render, make_row, build_procedural, and WORDS/template constants. They construct training and protected evaluation content together; supplied-frozen-input training does not need them. Excluded prepare functions: select_replay, main and the identity/context filtering chain; they require protected P2/probe/hold inputs and generate/reshuffle data instead of consuming the frozen records. Excluded execute functions from the public helper subset: generate, agg, evaluate, val500, baseline, part_a, final_eval; they are historical evaluation orchestration whose input and callback contracts follow below. The recovered execute.py in the prior transport remains reference source, not an independently portable launcher.

### Data and plan checks

SUPPLIED_INPUT_SCHEMAS_AND_CHECKS.json reports observed top-level field types/presence counts, bytes, hashes and encoding checks without row payloads or audit IDs. Native JSONL strings and record order must survive relocation unchanged. Training records include audit_id, family and messages; replay includes audit_id, messages and original source/task metadata. Stored formatted_tokens/shifted_targets are checked against source-preserving tokenization. All train/replay records fit 512 tokens, contain assistant content plus EOS supervised targets, and have disjoint audit IDs; replay is disjoint from P2 validation by audit ID. This is not a new semantic contamination audit.

UPDATE_PLAN.jsonl contains exactly 640 objects: update (1..640), epoch (1..2), block (1..64), stream (procedural/replay), row_ids (32 ordered unique string IDs). Each 10-update block has seven procedural and three replay updates. Each procedural update has eight records from each family in fixed order COPY, FIELD, MEMBERSHIP, JSON before the row shuffle. Each record appears twice across 20,480 exposures. The full original ordered plan equals helpers/p3_plan.build_plan(train,replay), using one random.Random(20260907), two epochs, original input order, and the original sequence of shuffles. Never substitute a regenerated data population or sort the input records. For future execution, load the supplied plan; derive it only as a verification and do not overwrite it.

### Training semantics for the laptop adapter

1. Require a fresh output directory, validated original input hashes, original P2 step750 hash 20afc40096568343c59d8a55e9606667c279f5f1c976908c57de18a0107f4e4e, and canonical tokenizer. Verify original manifests using an explicit original-logical-name-to-local-path resolver. Store its mapping separately; never rewrite FROZEN.json or claim the projection is the original. Historical verify_freeze also verifies all original runtime/preparation entries and accepted_source_files; a public subset validator must disclose which original checks it omits.
2. Future execution loads only P2 model/config with sft.train_sft.load_ckpt (CPU, weights_only=True, scoped NumPy uint32 RNG metadata allowlist), handles _orig_mod. prefixes as the historical load_model does, strictly loads GPT config/weights and requires 124,635,456 unique parameters/context 2048. No optimizer/scheduler/RNG state is restored from P2.
3. The historical training process sets CPU threads 4, torch/Python seed 20260907; after model loading it seeds torch/CUDA/Python again. Construct fresh AdamW over model.parameters(), lr 5e-5, betas (.9,.95), eps 1e-8, weight_decay 0; no fused option was explicitly requested in P3. Padding length 512, PAD=0, labels=-100; preserve messages and use default_system=None. The architecture remains context 2048.
4. Follow the supplied ordered row_ids. Split each 32-record update into 16 consecutive microbatches of two. Use src.canonical_schedule.lr_schedule(step-1,32,5e-5,schedule='cosine',schedule_total_steps=640), default min_lr_ratio=.1. The horizon is 640 even when step320 is the downstream parent; do not invent a separately scheduled 320-step run.
5. Use sft.train_sft.effective_batch_backward with BF16 CUDA forward and sft.p2_loss.assistant_nll_sum_fp32. FP32 selected-logit CE is summed over shifted assistant content and EOS; divide each microbatch numerator by the shared total targets for the whole update, with no extra division by 16. Validate row order and supervised target counts. checked_optimizer_step rejects nonfinite loss/gradients/norm, clips to 1, then steps once.
6. Save at 320/640 before their evaluations, with model, optimizer, config, completed step, 640-horizon scheduler, torch/CUDA/Python RNG, scaler=None, original run config/plan/freeze identity and status. Use exclusive launch/output files, atomic checkpoint save and a 7200-second process wall cap. Record failures; no automatic restart. Historical source uses per-step status/metrics and row/token exposure accounting.

Historical training actually completed 640 updates. Research-v1's downstream P3 parent is the saved step320 checkpoint; these are compatible facts. No training was started in this support task.

### Evaluation callbacks and mode differences

Offer distinct named modes, for example historical-schedule and training-only. The names are a proposed adapter interface, not historical function names.

Historical-schedule must require actual baseline evidence and execute development/validation hooks at 320/640. Preserve baseline() behavior: load P2, evaluate development.jsonl (512 rows), run val500 on P2 validation (500 rows, context2048), then evaluate TRAIN_SAMPLE128.jsonl. Only successful completion writes the new run's BASELINE_COMPLETE.json with status COMPLETE, optimizer_updates 0 and the P2 source hash. An existing historical marker is provenance, not approval for a new run; do not copy or fabricate it to bypass the gate.

A private hook provider should implement the original signatures evaluate(model,tok,rs,tag)->summary and val500(model,tok,tag)->position_nll_result, plus baseline orchestration. Original evaluate calls generate(model,tok,messages[:-1],64), then check(row,raw_text), writes one raw-output row per case and aggregates overall/family/condition and matched rendering pairs. Greedy temperature=0, top_k=0, top_p=1, EOS=3, BF16 autocast, no cleanup or constrained decoding. Fields passed privately include audit_id, family, condition, template_id, group_id, matched_payload_id, reference, messages, payload; these values are not distributed here.

check() payload schema depends on family: COPY uses value:string; FIELD uses fields:list of key/value pairs and key:string; MEMBERSHIP uses items:list[str], target:string, present_label:string, absent_label:string; JSON uses fields:list of key/value pairs and keys:list[str]. Actual labels, values and reference strings are owner-supplied private inputs. Checkers are exact-string/value diagnostics; do not substitute whitespace normalization or freeform semantic judging. val500 uses collate_fn_builder(tok,2048,None,False,1.0,[],'contains_any'), batch2, no shuffle/workers and evaluate_position_nll(...,expected_rows=500).

At step320/640 the original train function saves checkpoint, calls evaluate on development and val500, then resets peak memory accounting; next update returns the model to train mode. Baseline is a separate phase/process. Evaluation runtime counts toward the training-process wall cap. Omitting private hooks changes historical behavior and timing; training-only may explicitly omit them, must record baseline/development evaluation as NOT_RUN, and must not mint BASELINE_COMPLETE or claim full historical replay. It may separately retain the supplied P2 val500 hook if selected and recorded, but must disclose the precise hook set.

Full launch.py additionally runs final_eval, dev100 and likelihood after training. Final evaluation opens final.jsonl only after the training process ends; evaluates P2/P3 final, P3 train128 and Part A validation probes, then writes actual completion. Part A needs private probes with references and cached P2 outputs. dev100 requires the original evaluate_pilot_checkpoint.py; likelihood requires the pinned run_likelihood.py's Encoding, load_docs, prepare_requests, crosscheck, score_batch and format_doc plus its private datasets/protocol. These are evaluation-only, excluded from this training entry point. No final/benchmark scoring is authorized or performed here.

## Fixed interpolation contract

The transport already includes projected runtime/blend.py and common.py. Original-to-projection hashes are recorded separately. The actual numerical algorithm and preparation requirements are recoverable; no parent was loaded or blended here.

A new adapter must bind the two exact parent paths (P2 step750 then P3 step320), canonical tokenizer, fresh run/preparation/weights directories, source root and exclusive MATERIALIZATION_LAUNCH marker. Original blend.main does not create every directory itself. Supply only fixed alpha050=.50 and alpha075=.75; do not accept an optimization/search loop. The interpolation identity record pins parent hashes and historical candidate hashes; those hashes are prior evidence, not new measurements of those model files.

Preserve CPU FP32 theta_P2 + alpha*(theta_P3_step320-theta_P2). Endpoints 0/1 are arithmetic validation special cases, not additional output candidates. Require equal complete configs, state keys/shapes/dtypes, finite floating values and strict native model loading. Group parameters by original shared Parameter identity using named_parameters(remove_duplicate=False); compute once per alias group and reuse the same tensor for tok_emb.weight/lm_head.weight. Verify equality of tied parent entries, preserve sharing after save/reload and never mutate parent storage.

Fixed persistent buffers/nonfloating state must be equal and copied, never blended. Historical recorded fixed_state_keys was empty. The 60 nonpersistent RoPE buffers come from equal native configs and must match independently; they are not checkpoint parameters. Original checks include exact endpoints, small arithmetic checks, an FP64 convex-combination error bound of 4*FP32 epsilon*max(1,max_abs_oracle), strict save/reload equality and parent before/after digest/hash checks. Future adapters must run their own checks before claiming results; none ran here.

Only model/config/derivation are serialized for each inference-only candidate. Derivation records alpha, formula, parents, tokenizer hash, zero optimizer updates and source_training_step=None. Relocated parent paths change derivation metadata and may change serialized-file hashes without changing numerical tensors; preserve original identities and report new adapter/output identities separately. Parameter-state digests have their own sorted-key/dtype/shape/contiguous-byte algorithm in state_digest.

TASK/RET paths, owner-review markers and NEXT_ACTION/FLAGS are historical provenance. They are not numerical controls or a new optimization algorithm. Keep original task/freeze identities as provenance and use separately named adapter status files for new work. INFERENCE_FREEZE.json was for later evaluation and binds private evaluation inputs; it is not necessary to compute the two mathematical blends. Dropping that evaluation from a new materialization-only entry point must not be called full historical interpolation-and-evaluation replay. launch.py's native/dev100/likelihood jobs are outside this task.

## Native export: actual operations and complete builder boundary

The supplied build_bundle.py is a nine-line sanitized excerpt referring to external B/E; it is not a runnable builder. The original evidence/scripts/build_bundle.py has 17 physical lines and also depends on an existing PREFLIGHT record, bound package/evidence paths and prior directory setup. It copies private review/parity files as well as public assets, so it must not be republished wholesale as a public builder. Use the following source/output mapping for a new adapter, not exec of the excerpt.

| Source boundary | Public adapter operation | Output |
|---|---|---|
| Original build_bundle.py lines 5–11, 16–17 | Create fresh bundle/src; copy pinned model/chat_template/special_tokens and empty __init__; copy canonical tokenizer and recorded requirements. Historical accepted_generate gains only autocast_enabled argument and passes it to autocast. Prefer exact frozen bytes supplied here rather than repeating text replacement. | src/, tokenizer.json, requirements-inference-tested.txt |
| export_weights.py top-level conversion and checks | Verify alpha075 input hash/config guards; restricted accepted loader; strict GPT load; preserve full checkpoint config and tied FP32 state; safetensors.save_model then load_model(strict=True); compare every dtype/shape/value and alias/storage and native buffers. | model.safetensors, complete config.json, special_tokens_map.json; fresh conversion evidence |
| Existing frozen bundle inference.py and src/accepted_generate.py | Copy byte-for-byte; do not change generation behavior or inherit mutable source-tree modules. | inference.py and src/accepted_generate.py |
| document.py lines 43–47 and public documentation portion | Copy only already-public results if supplied; create new provenance/evaluation index from actual bound metadata and truthful statuses. Private review derivation at lines 6–42 is a separate optional private evidence workflow. | MODEL_PROVENANCE.json, EVALUATION_INDEX.json, README, SOURCE_NOTICE |
| finalize.py functions listing/manifest/pack_verify | Enumerate regular public output files only, exclude bytecode/self/enclosing archive from manifest, hash every file, pack, verify exact member set/sizes/hashes; produce external archive receipt. Do not execute its top-level evaluation assertions, moves or deletions. | MANIFEST.json, archive and checksum sidecar |

An independent export entry point takes an already supplied alpha075 checkpoint, original input bindings, a separate local path map, frozen source/tokenizer assets and a fresh out-dir. It must not reload interpolation parents or blend again. Pinned alpha075 hash is 1da85cc329d55e92dacf51c36623779558c4c6c9a39d78a34a064f61fcddbe97. Bindings' minimal dimension guards are not a substitute for complete checkpoint config (including rope fields/dropout). All 213 named FP32 state entries must survive numerical conversion; historical tied-aware safetensors stored 212 unique tensors, alias tok_emb.weight -> lm_head.weight, 124,635,456 unique parameters, 60 nonpersistent native RoPE buffers. These counts are historical records and future guards, not operations performed now.

frozen_native/ contains exact inference.py, accepted_generate.py, config.json, special_tokens_map.json and requirements bytes; it is a support subset, not a complete runnable inference bundle. Reuse model.py/chat_template.py/special_tokens.py and tokenizer.json from the already supplied verified transport, matching contracts/INTERPOLATION_EXPORT_IDENTITIES.json. The tokenizer hash is d8f84df58928023edebd809e152b3b38a0dac53b9f887bd2455f427661e9b9ce. Do not rewrite source-preserving formatting, precision profiles or greedy decoding. No weight bytes are included here. No inference module was imported or executed.

The frozen inference interface takes messages or prompt, model directory, profile bf16_native/fp32_math and cap 1..384 within context 2048. FP32 math disables autocast/TF32 and selects SDPA MATH; bf16_native preserves the recorded backend/profile flags. Keep those executable bytes exact; a surrounding builder may relocate paths without changing their hash.

Historical source/export parity and owner-review overlays are optional private evidence dependencies, not weight-conversion operations. No new parity, performance, baseline approval or final-evaluation claim can be inferred from copied records. A new technical export must mark parity NOT_RUN until actually verified under its own authorization. Omitting private review pages changes the historical evidence package, not the numerical export algorithm; document that scope. This task performs neither export nor model loading nor parity.

## Identity and stop conditions

MANIFEST.json hashes every public file except itself; the external SHA256 sidecar hashes the archive. SOURCE_HASH_MAP.json preserves original and new candidate identities plus exact selected source segments. Structured projections explicitly declare omitted fields and their own adapter identity. Never replace an original frozen hash with a projection hash to make verify_freeze pass.

P3_TRAINING_SUPPORT=READY_FOR_SUPPLIED_INPUT_ADAPTER; historical replay requires private hooks/inputs. INTERPOLATION_EXPORT_SUPPORT=CONTRACTS_AND_FROZEN_CODE_READY; public builders remain to implement on laptop. All five proposed deletion candidates and the optional 38.121GB batch were retained unchanged. No GitHub/HF writes, downloads, installation, training, checkpoint deserialization, model/optimizer construction, forward/backward, generation, scoring, CUDA operation or export occurred.
